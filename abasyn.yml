local_db:
  dsn: 'localhost:localtest1'
  user: 'sysdba'
  password: 'masterkey'

remote_db_query: 'SELECT remote_dsn, remote_user, remote_password FROM db_config WHERE id=1;'

logging:
  level: 'DEBUG'
  file: 'abasyn.log'
